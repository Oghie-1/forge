#!/usr/bin/env python3


"""This function is a utility for managing tire sensors"""
class SensorChecker:
    def __init__(self):
        if isinstance(self.sensor_array, (int, float)):
            return self.sensor_array